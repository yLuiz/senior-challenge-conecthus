# Em build remoto com EAS, configure estas variáveis no dashboard do EAS
# ou em mobile/eas.json. O mobile/.env pode não ser enviado se estiver ignorado.
# URL da API REST do backend (sem '/' no final)
# Produção (Railway): https://seu-backend.railway.app/api
# Local: http://localhost:3000/api
EXPO_PUBLIC_API_URL=https://your-backend.railway.app/api

# URL do broker MQTT via WebSocket
# Produção (HTTPS obrigatório wss://): wss://seu-broker.exemplo.com:8884
# Local: ws://localhost:9001
EXPO_PUBLIC_MQTT_URL=wss://your-mqtt-broker.example.com:8884
