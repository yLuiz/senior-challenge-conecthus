# URL completa da API — DEVE incluir o scheme (http:// ou https://)
# Sem o scheme, axios trata como caminho relativo e concatena com o domínio atual.
# Produção (Railway): https://seu-backend.up.railway.app/api
# Local: http://localhost:3000/api
VITE_API_URL=http://localhost:3000/api

# URL do broker MQTT via WebSocket
# Produção (HTTPS obrigatório wss://): wss://seu-broker.exemplo.com:8884
# Local: ws://localhost:9001
VITE_MQTT_URL=ws://localhost:9001